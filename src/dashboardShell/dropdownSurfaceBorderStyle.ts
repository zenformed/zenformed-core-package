import type { CSSProperties } from 'react';

/** Matches consuming app `accountMenuDropdown` panel border (see CSS_MODULE_CONTRACT). */
export const ZENFORMED_DROPDOWN_SURFACE_BORDER_STYLE: CSSProperties = {
  border: '1px solid var(--color-border, #e2e8f0)',
};
